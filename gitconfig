[user]
	email = james.dempsey420@gmail.com
	name = James Dempsey
[color]
	ui = true
[github]
	user = dempse35
[alias]
	last = log -1 HEAD
	st   = status
	ci   = commit
	co   = checkout
	br   = branch
	df   = diff
	sm   = submodule
	type = cat-file -t
	dump = cat-file -p
	hist = log --pretty=format:'%h %ad | %s%d [%an]' --graph --date=short
[core]
	editor = emacs
[filter "lfs"]
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
[credential]
	helper = osxkeychain
[tag]
	sort = version:refname
